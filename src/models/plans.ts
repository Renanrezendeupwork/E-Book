export type PlanType = {
  id: string;
  plan_id: string;
  payment_method: string;
  subtitle: string;
  days: number;
  price_amount: number;
  regular_price: number;
  students: number;
  name: string;
  sku: string;
};
